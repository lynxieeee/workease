import { ToolsConfig } from '@/typings/tools'

export const TOOLS:ToolsConfig = {
    "transfer-form-to-text": {
        title: 'Form Generation and Conversion',
        desc: 'form-filling utility that allows users to generate structured forms and automatically convert them into editable text formats.',
        imageUrl: ''
    }
}